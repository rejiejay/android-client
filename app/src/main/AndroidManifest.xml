<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.rejiejay.application">

    <!-- 所有的 安卓Activity视图 都需要在manifest里面进行配置、声明注册一下，才可以进行调用启动 -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyFullScreenTheme">

        <activity android:name=".MainActivity">
            <!-- 设置整个应用启动的第一个Activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name =".TabSelectActivity"/>
        <activity android:name =".DateSelectActivity"/>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>